import styled from 'styled-components';

export default styled.div`
    width: 200px;
    height: 250px;
    border-radius: 10px;
    border-radius: 10px;
    box-shadow: 1px 2px 1px rgba(0, 0, 0, 0.08);
    margin: 10px;
    cursor: pointer;

    :hover {
        box-shadow: 0 1px 12px 3px rgba(0, 0, 0, 0.08);
        transform: scale(1.01)
    }
`;